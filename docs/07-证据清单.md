# 07 · 证据清单（结论 → 原始证据对照）

> 复核方式：`_sources/` 下均为原始产物；反编译命令 `java -jar cfr.jar <class> --outputdir <dir>`。
> jar：`_sources/mc1218_client.jar`（1.21.8）、`C:\Users\Yuxiao Lu\AppData\Local\Temp\mc12111_client.jar`（1.21.11）、`...\mc262_client.jar`（26.2，命名版）。
> 解压缓存：`Temp\cbv\x12111full`（1.21.11 混淆）、`Temp\cbv\jar262full`（26.2 命名）。

## 1. 引擎结论

| 结论 | 证据 |
|---|---|
| 通道池 = ALC_MAX.sources，streaming=clamp(√total,2,8)，static=clamp(total−streaming,8,255)，fallback 30 | `_sources/dec12111b/fwh.java` 行 96-102,131-155（`i()` 读 4112）；`_sources/dec262/.../Library.java`（getChannelCount/池构造） |
| 池满 → acquire 返回 null → NOT_STARTED（无抢占） | `dec12111b/iqo.java` 行 358-365；`dec262/.../ChannelAccess.java` createHandle |
| 衰减距离 = max(volume,1)×sound.attenuationDistance | `dec12111b/iqo.java` 行 333-334（`$$5 = Math.max($$4,1.0f)*(float)$$3.i()`）；`dec262/.../SoundEngine.java` play() 同式 |
| 增益 = clamp(vol,0,1)×分类滑条×gainBySource | `dec12111b/iqo.java` 行 418-419；`dec262/.../SoundEngine.java` calculateVolume |
| pitch clamp [0.5,2.0]；MIN_SOURCE_LIFETIME=20；设备检查 1000ms | `dec12111b/iqo.java` 行 41-50；`dec262/.../SoundEngine.java` 常量区 |
| LINEAR 模型：AL_SOURCE_DISTANCE_MODEL=53251(AL_LINEAR_DISTANCE)、MAX=4131、REF=4129=1.0、ROLLOFF=4128=0 | `dec262/.../Channel.java` linearAttenuation 行 124-129；1.21.11 fwg 同结构 |
| 相对源 AL_SOURCE_RELATIVE=514；循环 4103；位置 4100；pitch 4099；gain 4106 | `dec262/.../Channel.java` 各行 |
| 流式 = 4×1s buffer 预灌 + updateStream 补灌；LoopingAudioStream 循环 | `dec262/.../Channel.java` attachBufferStream/pumpBuffers；`dec262/.../SoundBufferLibrary.java` getStream |
| ogg 纯 Java 解码（JOrbis），非关键 IO 池 | `dec262/.../SoundBufferLibrary.java` getCompleteBuffer/getStream |
| sounds.json 重载：全命名空间、Gson、FILE/SOUND_EVENT、replace、preload、Missing warn 一次 | `dec262/.../SoundManager.java` prepare/handleRegistration/apply；`dec12111b/iqr.java` 同构 |
| event 型引用的 volume/pitch 相乘（MultipliedFloats） | `dec262/.../SoundManager.java` handleRegistration SOUND_EVENT 分支 |
| listener = 相机位置，无速度（无多普勒） | `dec262/.../Minecraft.java` 行 1132（render 内 updateSource(mainCamera)）；`dec262/.../Listener.java`（仅 position/forward/up） |
| HRTF = ALC_HRTF 上下文属性（ALC_SOFT_HRTF 门控） | `dec12111b/fwh.java` 行 120-128；`dec262/.../Library.java` createAttributes |
| 暂停菜单 pauseAllExcept(MUSIC,UI) | `dec262/.../Minecraft.java` 行 1153 |
| F3 串 "Sounds: a/b + c/d" | `dec12111b/fwh.java` 行 233-235 |

## 2. 音符盒结论

| 结论 | 证据 |
|---|---|
| NOTE = IntegerProperty("note",0,24)；默认 HARP/0/false | `dec12111b/eox.java`（aV 行）；`dec262/.../BlockStateProperties.java` 行 140 |
| getPitchFromNote = 2^((n−12)/12) | `dec12111b/efi.java` 方法 `b(int)`；`dec262/.../NoteBlock.java` getPitchFromNote |
| NOTE_VOLUME=3；playSeededSound(RECORDS, 3.0f, pitch, seed) | `dec12111b/efi.java` 字段 e=3 + triggerEvent 调用行；`dec262/.../NoteBlock.java` 行 159 |
| 触发=红石上升沿/攻击/右键(+1%25)/blockEvent(0,0)；上方非 air/head 静默 | `dec12111b/efi.java` neighborChanged/useWithoutItem/onAttack/playNote |
| GameEvent.NOTE_BLOCK_PLAY + 音符粒子（y+1.2, 红=note/24） | `dec12111b/efi.java` playNote/triggerEvent |
| 23 乐器常量（16 base+6 head+custom）；26.2 = 27（+4 TRUMPET） | `dec12111b/epi.java` 全表；`dec262/.../NoteBlockInstrument.java` 全表 |
| block→乐器映射全表（1.21.11：644 默认 HARP + 显式映射） | `_sources/instrument-mapping-12111.txt`（从 `dec12111b/dzs.java` 提取）；交叉验证 `noteblock-wiki.txt` 行 217-718 乐器表 |
| 1.21.8 映射与 1.21.11 一致（oak_planks=BASS、copper_block=默认 HARP、gold_block=BELL） | `dec1218/dqb.java` 行 344/440/1298/1414 + `dec1218/efc.java` 枚举序 |
| 26.2 铜系四态 → TRUMPET 四态；pumpkin→DIDGERIDOO | `dec262c/.../Blocks.java` 行 1132-1146（switch WeatherState）、行 630 |
| 音符盒 48 格 = 3×16；唱机 64 = 4×16；服务端 range=16×vol | `dec262/.../SoundEvent.java` getRange；`dec262/.../SimpleSoundInstance.java` forJukeboxSong(4.0f) |
| 玩家头颅 NBT 自定义事件（SkullBlockEntity.getNoteBlockSound；null→不发声） | `dec12111b/efi.java` getCustomSoundId + triggerEvent 判空；`dec262/.../NoteBlock.java` 同 |
| 22 个 note_block 事件（1.21.11）/ 26 个（26.2）及文件映射（chime=icechime、creeper pitch0.5） | `_sources/vanilla-sounds-1.21.11.raw.json`、`_sources/vanilla-sounds-26.2.raw.json`、`_sources/note-entries-*.txt`（Mojang CDN 原始文件） |
| 采样率：bd/bassattack/snare/hat/pling=44.1k，其余=48k（单声道 Vorbis） | `_sources/ogginfo2.ps1` + `ogg2.ps1` 实测输出（Vorbis identification header 解析） |
| 唱机 JukeboxSongPlayer/JukeboxSong 组件（26.2） | `dec262b/.../JukeboxBlockEntity.java`、`dec262b/.../JukeboxSongPlayer.java` |

## 3. 历史/外部来源

| 结论 | 证据 |
|---|---|
| 1.21.11 发布线：1.21.9/10/11 正式版，26.1(.1/.2)/26.2 已发布、26.3 快照 | `_sources/mcmeta-versions.json`（Mojang version manifest） |
| 26.1 snap7 新增 Trumpet 乐器 + 铜块改 trumpet；snap11 更新音频 | `_sources/noteblock-wiki.txt` 行 943-945（Java 历史表，抓取 2026-08） |
| 19w09a 加 iron_xylophone/cow_bell/didgeridoo/bit/banjo/pling；17w16a 事件；22w46a 头部模仿 | `_sources/noteblock-wiki.txt` 行 920-939 |
| 音符盒 48 格可闻、RECORDS 滑条名 "Jukebox/Note Blocks"、25 音高 400 音符、粒子颜色 | `_sources/noteblock-wiki.txt` 行 48-56 |
| 悦灵 16 格寻路/30s 机制、观察者触发、F3 note 值 | `_sources/noteblock-wiki.txt` 行 762/772/215 |
| Fabric 工具链：loader 0.19.5、yarn 1.21.11+build.6、fabric-api 0.141.6+1.21.11、Loom 1.17.x 稳定线、mixin 0.8.7 | meta.fabricmc.net/v2/versions/loader/1.21.11、/yarn/1.21.11；api.modrinth.com fabric-api 版本 API；maven.fabricmc.net loom metadata（均 2026-08 查询） |
| 1.21.9+ Fabric 文档默认 Mojang mappings | docs.fabricmc.net/1.21.11/develop/getting-started/creating-a-project |

## 4. loopdetect 真实样本首测（2026-08）

| 结论 | 证据 |
|---|---|
| 用户库 21 个：PASS 7 / WARN 1 / FAIL 2 / 拒 11；原版 16 个：PASS 7 / WARN 1 / FAIL 1 / 拒 7；26.2 trumpet 4/4 全拒 | `_sources/loopdetect-run/REPORT.md` + `result-*.json`（原始 JSON 可复核） |
| 工具 = Hy4 侧新版（原生采样率自适应）；参数默认 + `--total 12`（长起音 render 越界修复）；Python 3.13.3 + numpy/scipy/soundfile | `_sources/loopdetect-run/loopdetect.py`、`REPORT.md` §5 复现命令 |
| 样本来源 = 用户 Note Block Studio 库（`data\sounds\*.ogg`，已含 26.1 trumpet×4）+ Mojang CDN 原版 note/*.ogg | `_sources/collect-samples.ps1`；`C:\Users\Yuxiao Lu\AppData\Roaming\Minecraft_Note_Block_Studio\data\sounds` |

## 4b. L0 样本管线首验（2026-08，docs/09）

| 结论 | 证据 |
|---|---|
| 用户 harp 整形后 1.95pp/−36.8dBc ≈ Hy4 合成模型 1.93dB（管线交叉验证通过） | `_sources/loopdetect-run/verify-user.json`（harp 行）；对照模型值见 `_hy4/导出区/对话记录` 第2轮 + 2026-08 修正 |
| 60ms 为全局最优长度（40/50/60/75/90/120ms 扫描）；trumpet_oxidized 最优 75ms（14.3→3.99pp） | `verify-sweeps/*.json`（17 个边界样本扫描） |
| 全库 21 样本 20Hz 重触发模拟（原始 vs 整形，5 音高）；打击乐 60ms 平台无效 | `verify-user.json`、`verify-user.txt` |
| 原版 16 样本同法对照（icechime 原版 1.39/−53.7 远优于用户版） | `verify-vanilla.json`、`verify-vanilla.txt` |
| 整形产出 21×(wav+ogg)，保留原生采样率，回读验证首尾趋零/时长精确 | `shaped-user/`（含 `l0-meta.json`） |
| .nbs = Open NBS v5 格式，pynbs 库解析；不存坐标，物理盒由 (layer,instrument) 确定；20 乐谱 L1 统计 | `_sources/pynbs-file.py`（pynbs 源码存档）、`nbs_stat.json`、`nbs_stat.txt`、`tools/nbs_stat.py` |
| 工具：l0shape.py（整形）/ verify_l0.py（20Hz 重触发模拟）/ nbs_stat.py（乐谱统计） | `tools/` 三脚本 + docs/09 §10 复现命令 |

## 5. Hy4 材料（`_hy4/导出区/` + 2026-08 第二轮修正答复）

| 文件 | 用途 |
|---|---|
| 对话记录_Minecraft红石音乐声学优化.md | 3 轮讨论：叠加成因、60ms+8ms 包络、voice 合并、延音方案 |
| loopdetect.py | 循环点检测（f0+NCC≥0.95 → attackEnd/loopStart/loopEnd @44.1k 基准） |
| final_metric.png / legato_mod.png / loopalgo.png | 20Hz 叠加失真对比（−19.8/−12.8/−11.5/−27.3 dBc）、包络方案、算法示意 |
